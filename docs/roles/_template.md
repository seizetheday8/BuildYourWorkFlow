# <节点 ID>

> 这是一张角色卡。它描述"这个端在这个环境里是谁、能做什么、怎么被唤醒"。

## 身份

- **ID**：`<短标识，如 ds-web / cc-local>`
- **类型**：网页端 / 本地 CLI / 本地 IDE / Agent 运行时
- **模型**：`<模型名>`

## 能力（当前实际可用）

- **读文件**：是 / 否
- **写文件**：是 / 否
- **执行命令**：是 / 否
- **联网搜索**：是 / 否
- **擅长**：`<一句话>`
- **不擅长**：`<一句话>`

> 若某项能力依赖外部条件（如桥接插件），在字段后用括号说明。

## 成本

- **计费方式**：免费（订阅 / 免费额度）/ 按量（API）/ 订阅内
- **相对成本**：低 / 中 / 高
- **备注**：`<可选>`

## 唤醒方式

- **人工触发**：`<怎么触发，如"贴握手模板 + 仓库地址">`
- **自动触发**：否 / CDP / IPC / HTTP
- **备注**：`<可选>`

## 当前状态

- **活跃**：是 / 否
- **最后参与**：`YYYY-MM-DD`
- **当前任务**：`<无 / 描述>`

---

*最后更新：YYYY-MM-DD*
